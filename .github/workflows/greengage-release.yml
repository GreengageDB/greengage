# .github/workflows/greengage-release.yml
name: Greengage release

on:
  release:
    types: [released]

jobs:
  upload-to-release:
    strategy:
      fail-fast: false
      matrix:
        include:
        - target_os: ubuntu
          target_os_version: '22.04'
          extensions:  deb ddeb
        - target_os: ubuntu
          target_os_version: '24.04'
          extensions:  deb ddeb
        - target_os: rockylinux
          target_os_version: '8'
          extensions:  rpm
        - target_os: rockylinux
          target_os_version: '9'
          extensions:  rpm
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      actions: read
    steps:
      - name: Upload packages to release
        uses: greengagedb/greengage-ci/.github/actions/upload-pkgs-to-release@v47
        with:
          extensions:        ${{ matrix.extensions }}
          target_os:         ${{ matrix.target_os }}
          target_os_version: ${{ matrix.target_os_version }}
          artifact_prefix:   Package
