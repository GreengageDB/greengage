name: Greengage Auto Build

env:
  version: 7

on:
  push:
    tags: ['${{ env.version }}.*']
  pull_request:
    branches: ['*']
    tags: ['${{ env.version }}.*']

jobs:
  build-ubuntu:
    # Job for building on Ubuntu, runs in parallel with other jobs
    uses: GreengageDB/greengage-ci/.github/workflows/greengage-reusable-build.yml@main
    with:
      version: ${{ env.version }}
      target_os: ubuntu
      target_os_version: ''
    secrets:
      # GitHub token for authentication
      ghcr_token: ${{ secrets.GITHUB_TOKEN }}

  # build-rockylinux:
  #   # Job for building on Rocky Linux, runs in parallel with other jobs
  #   uses: GreengageDB/greengage-ci/.github/workflows/greengage-reusable-build.yml@main
  #   with:
  #     version: ${{ env.version }}
  #     target_os: rockylinux
  #     target_os_version: 8
  #   secrets:
  #     # GitHub token for authentication
  #     ghcr_token: ${{ secrets.GITHUB_TOKEN }}
